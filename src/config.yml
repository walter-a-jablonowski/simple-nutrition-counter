
devMode:   true         # show new ui elems that are in dev
tryDesign: true         # try new UI (see controller.php)

user:      single_user

substances:             # TASK: rename

  gluten:  ['weizen']
  lactose: ['milch']

highIntake:             # TASK: maybe move in nutrients

  fat: 100

defaultSettings:        # see also users > settings.yml

  layout:

    useCoffeeCounter:  true
    useFillupsCounter: true

  foods:

    defaultAmounts:
      
      pack:   ["1/4", "1/3", "1"]
      pieces: [1, 2, 3]
