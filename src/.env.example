# Secrets, read via env_get() (src/lib/env.php). Copy to .env and fill in.
# Never commit the .env itself.

# Google AI Studio key for the voice agent (https://aistudio.google.com/apikey).
# Used server side only: the browser gets a short lived ephemeral token instead.
GEMINI_API_KEY=
